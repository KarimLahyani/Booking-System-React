export const metadata = {
  title: "My Reservations | Booking System",
  description: "View and manage your hotel reservations."
};

export default function ReservationsLayout({ children }) {
  return children;
}
